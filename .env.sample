# for the app
# Whether the app is hosted behind a reverse proxy
#PROXY=true
PORT=3000
#BASE_URL=https://example.com
# App will make requests using this WebId - permissions will need to be granted
# If missing, requests will be made as the soap:isActorOf - solid:oidcIssuer then needs to be specified
# WEB_ID='https://example.com/profile/card#bot'

# for a script
#CSS_IDENTITY_PROVIDER=
#CSS_IDENTITY_EMAIL=
#CSS_IDENTITY_PASSWORD=
